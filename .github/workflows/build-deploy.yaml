name: Build and Deploy

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Run lint
        run: npm run lint

  test:
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Run Unit tests
        run: npm run test

  prepare-image-metadata:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: [test]
    outputs:
      calver_tag: ${{ steps.calver.outputs.tag }}
      sha_tag: ${{ steps.sha.outputs.tag }}
      image_labels: ${{ steps.image_metadata.outputs.labels }}
    steps:
      - name: Generate calver tag
        id: calver
        run: |
          echo "tag=$(date +%Y-%m)-build" >> $GITHUB_OUTPUT
      - name: Generate sha tag
        id: sha
        run: |
          echo "tag=sha-${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
      - name: Docker metadata
        id: image_metadata
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/15ajohnson/wedding
          tags: |
            type=raw,value=${{ steps.calver.outputs.tag }}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha

  build-arch:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            platform: linux/amd64
            arch: amd64
          - runner: ubuntu-24.04-arm
            platform: linux/arm64
            arch: arm64
    runs-on: ${{ matrix.runner }}
    needs: [prepare-image-metadata]
    permissions:
      packages: write
    outputs:
      amd64_digest: ${{ steps.collect_digest.outputs.amd64_digest }}
      arm64_digest: ${{ steps.collect_digest.outputs.arm64_digest }}
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Docker build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          file: Dockerfile
          labels: ${{ needs.prepare-image-metadata.outputs.image_labels }}
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=ghcr.io/15ajohnson/wedding,push-by-digest=true,name-canonical=true,push=true
      - name: Collect digest output
        id: collect_digest
        run: |
          # Emit one uniquely named output key per architecture so matrix outputs can be merged.
          echo "${{ matrix.arch }}_digest=${{ steps.build.outputs.digest }}" >> "$GITHUB_OUTPUT"

  build-manifest:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: [prepare-image-metadata, build-arch]
    permissions:
      packages: write
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Create and push multi-arch manifests
        run: |
          # Build canonical digest references from explicit per-arch job outputs.
          amd64_digest="${{ needs.build-arch.outputs.amd64_digest }}"
          arm64_digest="${{ needs.build-arch.outputs.arm64_digest }}"

          if [[ -z "$amd64_digest" || -z "$arm64_digest" ]]; then
            echo "Missing matrix digest outputs. amd64='$amd64_digest' arm64='$arm64_digest'"
            exit 1
          fi

          amd64_source="ghcr.io/15ajohnson/wedding@$amd64_digest"
          arm64_source="ghcr.io/15ajohnson/wedding@$arm64_digest"

          # Publish the calver multi-arch tag.
          docker buildx imagetools create \
            --tag ghcr.io/15ajohnson/wedding:${{ needs.prepare-image-metadata.outputs.calver_tag }} \
            "$amd64_source" \
            "$arm64_source"

          # Publish/refresh the latest multi-arch tag.
          docker buildx imagetools create \
            --tag ghcr.io/15ajohnson/wedding:latest \
            "$amd64_source" \
            "$arm64_source"

          # Publish an immutable commit-derived multi-arch tag.
          docker buildx imagetools create \
            --tag ghcr.io/15ajohnson/wedding:${{ needs.prepare-image-metadata.outputs.sha_tag }} \
            "$amd64_source" \
            "$arm64_source"

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs: [build-manifest]
    steps:
      - name: Configure ssh key
        env:
          PRIVATE_KEY: ${{ secrets.WEDDING_PRIVATE_KEY }}
        run: |
          echo "$PRIVATE_KEY" > id_rsa_wedding
          chmod 600 id_rsa_wedding
      - name: Docker Compose up
        env:
          HOST: "163.192.215.50"
        run: |
          ssh -i id_rsa_wedding -o "StrictHostKeyChecking no" wedding@$HOST "
            curl -o docker-compose.yaml https://raw.githubusercontent.com/15ajohnson/collin-alivia-wedding/main/docker-compose.yaml
            docker compose pull
            docker compose up -d
          "
